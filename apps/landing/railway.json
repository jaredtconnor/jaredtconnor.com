  {
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "cd ../.. && cp pnpm-lock.yaml apps/landing/ && pnpm install --frozen-lockfile && pnpm --filter landing build"
  },
  "deploy": {
    "startCommand": "pnpm preview",
    "restartPolicyType": "ON_FAILURE"
  }
}
