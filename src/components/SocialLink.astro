---
export interface Props {
  href: string;
  icon: string;
}

const { href, icon } = Astro.props;
---

<a href={href} class="lg:py-[5px] md:pl-[5px]">
  <span class="icon transition hover:bg-sky-200 hover:text-slate-800">
    <i class={icon}></i>
    <slot />
  </span>
</a>
