import Link from 'next/link';
import { SITE, SOCIALS } from '@/app/(landing)/const';

export default function Footer() {
  return (
    <footer className="py-8 mt-12 border-t border-gray-200 dark:border-gray-800"> 
    <div>Footer</div>
    </footer>
  );
}