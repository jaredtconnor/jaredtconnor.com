# Database Configuration
# Use Neon PostgreSQL for all environments
DATABASE_URL="postgresql://username:password@hostname:5432/database_name?sslmode=require"

# NextAuth Configuration (for blog authentication)
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="your-nextauth-secret-here-minimum-32-characters"
NEXTAUTH_URL="http://localhost:3002"

# PayloadCMS Configuration
# Generate with: openssl rand -base64 32
PAYLOAD_SECRET="your-payload-secret-here-minimum-32-characters" 
PAYLOAD_PUBLIC_SERVER_URL="http://localhost:3000"

# API Configuration
NEXT_PUBLIC_API_URL="http://localhost:3000"
API_URL="http://localhost:3000"

# AWS Configuration (for SST deployment)
# Leave blank for local development - only needed for CI/CD
AWS_ACCESS_KEY_ID=""
AWS_SECRET_ACCESS_KEY=""
AWS_REGION="us-west-2"

# Media Storage (auto-configured by SST)
MEDIA_BUCKET_NAME=""

# Environment
NODE_ENV="development"

# Development Ports (optional)
PORT=3000
